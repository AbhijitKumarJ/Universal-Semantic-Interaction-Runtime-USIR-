{
  "name": "@usir/runtime",
  "version": "0.1.0",
  "description": "The brain of USIR — Interaction Memory, Intent Router, Topological Executor, Disambiguation, Provenance",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc -w",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@usir/protocol": "workspace:*",
    "@usir/federation": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
