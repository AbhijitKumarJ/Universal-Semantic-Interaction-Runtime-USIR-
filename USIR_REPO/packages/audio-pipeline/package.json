{
  "name": "@usir/audio-pipeline",
  "version": "0.1.0",
  "description": "Audio ingestion: VAD + Whisper STT + FusedIntent (linguistic + pointing + implicit)",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc -w",
    "clean": "rm -rf dist",
    "test": "echo 'no tests yet'",
    "lint": "echo 'no lint yet'",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@usir/protocol": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
